import React, {Component} from 'react';import axios from 'axios';export default class Listing extends Component {    constructor() {        super();        this.state = {            cars: []        }    }    /**     * Get Cars from db     */    componentDidMount() {        axios.get('http://megakit/cars')            .then(response => {                this.setState({cars: response.data});            })    }    render() {        return (            <div>                <table className="table">                    <thead className="thead-dark">                    <tr>                        <th scope="col">#</th>                        <th scope="col">Make</th>                        <th scope="col">Model</th>                        <th scope="col">Driver / User</th>                        <th scope="col">Control</th>                    </tr>                    </thead>                    <tbody>                    {                        this.state.cars.map(car => {                            return (                                <tr key={car.id}>                                    <th scope="row">1</th>                                    <td>{car.make}</td>                                    <td>{car.model}</td>                                    <td>{car.user.name}</td>                                    <td>Control</td>                                </tr>                            )                        })                    }                    </tbody>                </table>            </div>        );    }}