import React, {Component} from 'react';import axios from "axios";export default class Create extends Component {    constructor() {        super();        this.handleInputChange = this.handleInputChange.bind(this);        this.onSubmit = this.onSubmit.bind(this);        this.state = {            name: '',            email: '',            password: '',            confirmation: '',            cars_id: ''        }    }    /**     *     * @param event     */    handleInputChange(event) {        const target = event.target;        this.setState({            [target.name]: target.value        });    }    /**     *     * @param event     */    onSubmit(event) {        event.preventDefault();        const user = {user: this.state};        console.log(user);        // return;        axios.post('http://megakit/api/save/user', user)            .then(response => {                console.log(response.data);            })    }    render() {        return (            <div className="row justify-content-center">                <div className="col-md-6 col-sm-8">                    <form onSubmit={this.onSubmit}>                        <div className="form-group">                            <label htmlFor="userName">User name</label>                            <input type="text" name="name" className="form-control" id="userName" required                                   onChange={this.handleInputChange} value={this.state.name} placeholder="Ivan"/>                        </div>                        <div className="form-group">                            <label htmlFor="userEmail">Email</label>                            <input type="email" name="email" className="form-control" id="userEmail"                                   onChange={this.handleInputChange} placeholder="megakit@gmail.com"                                   value={this.state.email} required/>                        </div>                        <div className="form-group">                            <label htmlFor="userPassword">Password</label>                            <input type="password" name="password" className="form-control" id="userPassword"                                   onChange={this.handleInputChange} value={this.state.password} required/>                        </div>                        <div className="form-group">                            <label htmlFor="userConfirmation">Confirm password</label>                            <input type="password" name="confirmation" className="form-control" id="userConfirmation"                                   onChange={this.handleInputChange} value={this.state.confirmation} required/>                        </div>                        <div className="form-group">                            <label htmlFor="userCar">Cars</label>                            <select className="form-control" onChange={this.handleInputChange} name="cars_id"                                    id="userCar" required>                                <option>1</option>                                <option>2</option>                            </select>                        </div>                        <button type="submit" className="btn btn-primary">Create User</button>                    </form>                </div>            </div>        );    }}